spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/gridmind_db
    username: postgres
    password: Cristian17*
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080

gemini:
  api:
    key: "AIzaSyCfTu35ZT2VWO_ICZQ-HewhTFHUGBCc5KM"
