spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/gridmind_db
    username: postgres
    password: Cristian17*
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080

gemini:
  api:
    key: ${GEMINI_API_KEY}

app:
  frontend:
    url: ${FRONTEND_URL:http://localhost:5173}
  resend:
    api-key: ${RESEND_API_KEY}
    from-email: "GridMind <onboarding@resend.dev>"
